# Tier B flow #3 — background/foreground lifecycle.
#
# Enrolls fresh, sends a self-DM (so there's a row to look for after
# relaunch), backgrounds + relaunches the app, asserts identity + the
# row are restored.

appId: xyz.speakeasyapp.app
---
- launchApp:
    clearState: true
    clearKeychain: true
- runFlow:
    when:
      visible: "Wait"
    commands:
      - tapOn: "Wait"
- extendedWaitUntil:
    visible:
      id: "onboarding-continue"
    timeout: 30000
- tapOn: "onboarding-continue"
# Handle IdReveal if it appears
- runFlow:
    when:
      visible:
        id: "id-reveal-get-started"
    commands:
      - tapOn: "id-reveal-get-started"
# Enroll lands on Conversations. Capture userId from header.
- extendedWaitUntil:
    visible:
      id: "conversations-userid"
    timeout: 15000
- copyTextFrom:
    id: "conversations-userid"
- tapOn: "conversations-new-chat"
- tapOn: "new-chat-peer-id-input"
- pasteText
- tapOn: "new-chat-start"
- extendedWaitUntil:
    visible:
      id: "chat-input"
    timeout: 10000
- tapOn: "chat-input"
- inputText: "before background"
- tapOn: "chat-send"
- extendedWaitUntil:
    visible: "before background"
    timeout: 10000
- tapOn: "chat-back"
- waitForAnimationToEnd
# Background + relaunch (clearState: false so AsyncStorage persistence
# is exercised — this is the actual behavior under test).
- pressKey: Home
- waitForAnimationToEnd
- launchApp:
    clearState: false
- runFlow:
    when:
      visible: "Wait"
    commands:
      - tapOn: "Wait"
- extendedWaitUntil:
    visible:
      id: "conversations-userid"
    timeout: 10000
- extendedWaitUntil:
    visible: "before background"
    timeout: 5000
