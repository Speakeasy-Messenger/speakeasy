# Tier B flow #4 — peer-not-found surfaces at chat-open.

appId: xyz.speakeasyapp.app
---
- launchApp:
    clearState: true
    clearKeychain: true
- runFlow:
    when:
      visible: "Wait"
    commands:
      - tapOn: "Wait"
- extendedWaitUntil:
    visible:
      id: "onboarding-continue"
    timeout: 30000
- tapOn: "onboarding-continue"
# Handle IdReveal if it appears
- runFlow:
    when:
      visible:
        id: "id-reveal-get-started"
    commands:
      - tapOn: "id-reveal-get-started"
- extendedWaitUntil:
    visible:
      id: "conversations-userid"
    timeout: 15000
- tapOn: "conversations-new-chat"
- assertVisible:
    id: "new-chat-peer-id-input"
- tapOn: "new-chat-peer-id-input"
- inputText: "definitely-fake-userid"
- tapOn: "new-chat-start"
- extendedWaitUntil:
    visible: "No such user.*"
    timeout: 8000
- assertNotVisible:
    id: "chat-input"
