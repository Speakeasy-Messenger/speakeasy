appId: xyz.speakeasyapp.app
name: Enroll fresh user (Tier B)
---

# 1. Clear state & launch
- clearState
- launchApp

# 1a. Dismiss the keyguard PIN that re-engaged on clearState.
- runFlow: _dismiss-keyguard.yaml

# 2. Dismiss ANR dialog if present
- tapOn:
    text: "Wait"
    optional: true

# 3. Wait for onboarding screen
- extendedWaitUntil:
    visible:
      id: "onboarding-continue"
    timeout: 20000

# 4. Pick a handle — Continue is disabled until availability=available.
- tapOn:
    id: "onboarding-handle"
- inputText: "tierbone"
- extendedWaitUntil:
    visible: "@tierbone is available."
    timeout: 10000

# 4b. Tap Continue to begin enrollment
- tapOn:
    text: "Continue"

# 4a. Vouchflow's verify() shows BiometricPrompt; emulator has a PIN
#     but no fingerprint, so Android falls back to PIN entry from
#     com.android.systemui. Type 0000 to dismiss.
- runFlow: _unlock-biometric-prompt.yaml

# 5. Wait for enrollment to complete
#    NOTE: Race condition — after setUserId(), the navigator flips
#    to the Auth stack (initialRoute: Conversations). The
#    navigation.replace('IdReveal') in OnboardingScreen may lose
#    the race, so we skip IdReveal and wait directly for Conversations.
- extendedWaitUntil:
    visible:
      id: "conversations-userid"
    timeout: 30000
