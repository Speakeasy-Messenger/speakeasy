/**
 * Stub for @react-navigation/native in vitest.
 * Only the type imports used by push-handler.ts need to resolve.
 */
export function createNavigationContainerRef() {
  return { current: null };
}
