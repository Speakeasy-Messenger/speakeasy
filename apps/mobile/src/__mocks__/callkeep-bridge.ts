/**
 * Stub for callkeep-bridge in vitest.
 * The real module imports react-native-callkeep which has native side effects.
 */
export class CallKeepBridge {
  constructor(_opts: unknown) {}
  async start() {}
  async end() {}
}
