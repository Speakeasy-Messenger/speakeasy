// Removed per CLAUDECODENOTE.md item 6 — GIFs contradict the brand
// thesis (third-party CDN privacy leak, type discipline, moderation
// surface inheritance, viral mechanics). The component, the GifIcon,
// and the Tenor client are all retired. Empty file kept until `rm`.
export {};
