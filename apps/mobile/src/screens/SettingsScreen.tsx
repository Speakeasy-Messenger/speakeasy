// Removed per SETTINGS.md restructure — the flat one-screen
// settings was replaced by an 8-surface tree (landing + Privacy /
// Notifications / Appearance / Account / DeleteAccount / About,
// plus AvatarPicker drilldown). The new entry point is
// `<SettingsLandingScreen />`. File kept until `rm`.
export {};
